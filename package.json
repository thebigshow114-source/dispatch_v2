{
  "name": "kitchen-plan-extractor",
  "version": "1.0.0",
  "description": "AI-powered kitchen plan data extractor using Groq + pdf-parse",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.1",
    "pdf-parse": "^1.1.1"
  }
}
